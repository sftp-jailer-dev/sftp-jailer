---
phase: 06-v1-1-carry-over-closure-firewall-edge-tui-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/sysops/real.go
  - internal/sysops/sysops.go
  - internal/sysops/real_test.go
  - internal/tui/screens/addkey/addkey.go
  - internal/tui/screens/addkey/addkey_test.go
  - internal/tui/screens/deleteuser/deleteuser.go
  - internal/tui/screens/deleteuser/deleteuser_test.go
  - internal/tui/screens/applysetup/applysetup.go
  - internal/tui/screens/applysetup/applysetup_test.go
  - internal/tui/screens/pwauthdisable/pwauthdisable.go
  - internal/tui/screens/pwauthdisable/pwauthdisable_test.go
autonomous: true
requirements: [TUI-11]
tags: [tui, modal, cancellation, sysops, sigterm]

must_haves:
  truths:
    - "internal/sysops/real.go::Exec sets cmd.Cancel to send SIGTERM (NOT the default SIGKILL) and cmd.WaitDelay = 2s for the SIGKILL fallback (Cancel pattern stable since Go 1.20)."
    - "Exec correctly classifies a successful exit-after-Cancel as cancellation, not failure: Cancel returns nil when underlying Signal returns os.ErrProcessDone (Pitfall 1)."
    - "ExecResult carries the live subprocess PID (cmd.Process.Pid captured after cmd.Start) so callers can render PID-bearing diagnostics on hang (D-08)."
    - "All 9 detached context.Background() sites in the 4 TUI-11 modal files are replaced with stored cancellable contexts (context.WithCancel + WithTimeout for the deadline)."
    - "Each of the 4 modals (addkey, deleteuser, applysetup, pwauthdisable) routes Esc during a phaseCommitting / phaseFetching / runPreflight phase to call cancelFn; modal STAYS OPEN per D-07 and closes only when committedMsg arrives."
    - "During cancellation phase (after Esc, before subprocess exits), the modal View() renders a 'Cancelling...' indicator (D-07)."
    - "Cancellation handler distinguishes context.Canceled-on-cancellation from raw subprocess error: m.cancelling==true + (err==nil OR errors.Is(err, context.Canceled)) renders neutral 'cancelled' state, NOT success or fatal error (Pitfall 2)."
    - "When SIGKILL fails to release the subprocess within 2s (hang escalation, D-08), the modal renders 'Cancellation failed - subprocess PID=<N> still alive. Run kill -9 <N> from another shell.' with the live PID injected."
    - "SAFE-04 revert window contract preserved unchanged for any FW mutations in the cancellation paths (D-21)."
  artifacts:
    - path: "internal/sysops/sysops.go"
      provides: "ExecResult gains PID int field"
      contains: "PID"
    - path: "internal/sysops/real.go"
      provides: "Exec sets cmd.Cancel = SIGTERM closure + cmd.WaitDelay = 2s; populates ExecResult.PID after cmd.Start"
      contains: "cmd.Cancel"
    - path: "internal/sysops/real_test.go"
      provides: "TestExec_cancel_sends_SIGTERM_then_SIGKILL_after_2s using /bin/sleep; TestExec_returns_pid_after_start"
    - path: "internal/tui/screens/addkey/addkey.go"
      provides: "stored cancelFn at lines 380 and 532; Esc-during-phase handler invokes it; Cancelling indicator + D-08 hang diagnostic"
    - path: "internal/tui/screens/deleteuser/deleteuser.go"
      provides: "stored cancelFn at lines 229 and 464; Esc-during-phase handler; Cancelling indicator + D-08 hang diagnostic"
    - path: "internal/tui/screens/applysetup/applysetup.go"
      provides: "stored cancelFn at lines 290, 491, 545; Esc-during-phase handler; Cancelling indicator + D-08 hang diagnostic"
    - path: "internal/tui/screens/pwauthdisable/pwauthdisable.go"
      provides: "stored cancelFn at lines 290 and 488; Esc-during-phase handler; Cancelling indicator + D-08 hang diagnostic"
  key_links:
    - from: "internal/sysops/real.go::Exec"
      to: "syscall.SIGTERM via cmd.Cancel closure + cmd.WaitDelay = 2s"
      via: "single edit between exec.CommandContext and cmd.CombinedOutput (or cmd.Start + cmd.Wait when capturing PID)"
      pattern: "cmd\\.Cancel.*SIGTERM"
    - from: "internal/sysops/real.go::Exec"
      to: "ExecResult.PID populated from cmd.Process.Pid after cmd.Start"
      via: "result.PID = cmd.Process.Pid"
      pattern: "cmd\\.Process\\.Pid"
    - from: "each modal's done-msg goroutine"
      to: "done-msg PID field populated from ExecResult.PID"
      via: "msg.pid = result.PID (or msg.pid = lastPID captured in goroutine before send)"
      pattern: "pid +int"
    - from: "each modal's handleKey case esc during async phase"
      to: "stored m.cancelFn func from context.WithCancel"
      via: "if m.cancelFn != nil { m.cancelFn() }; m.cancelling = true; modal stays open per D-07"
      pattern: "m\\.cancelFn\\(\\)"
    - from: "each modal's handleCommitted (or equivalent done-handler)"
      to: "neutral cancelled-state branch"
      via: "if m.cancelling && (err==nil || errors.Is(err, context.Canceled)) -> render 'cancelled by Esc'"
      pattern: "errors\\.Is\\(.*context\\.Canceled\\)"
    - from: "each modal's handleCommitted hang branch"
      to: "D-08 verbatim diagnostic copy with live PID"
      via: "fmt.Sprintf(\"Cancellation failed - subprocess PID=%d still alive. Run kill -9 %d from another shell.\", pid, pid)"
      pattern: "Run kill -9"
    - from: "each modal's View() during phaseCancelling"
      to: "'Cancelling...' indicator render"
      via: "lipgloss render of static text 'Cancelling...' (or spinner+text) when m.cancelling==true"
      pattern: "Cancelling"
---

<objective>
Replace the 9 detached context.Background() sites in the 4 mutation modals (addkey, deleteuser, applysetup, pwauthdisable) with stored cancellable contexts. Route Esc during async operations to call cancelFn so SIGTERM is delivered to the in-flight subprocess; cmd.WaitDelay = 2s ensures SIGKILL fallback if the subprocess doesn't exit gracefully.

Per the research recommendation (RESEARCH §"Primary recommendation"): the sysops Cancel/WaitDelay change is the architectural foundation - it MUST land in this same plan as the 9 site replacements so the modal goroutines pass a context to a sysops that honors SIGTERM grace. If the sysops change shipped after the modal plumbing, cancellation would SIGKILL immediately (default Cancel behaviour) and the 2s grace would not kick in.

Per D-09: planner enumerates the final list against the codebase. Verified 9 sites (research found 9; D-09 mentioned 7 - the planner-time enumeration is the source of truth):
  - addkey/addkey.go:380 (resolveAsync)
  - addkey/addkey.go:532 (attemptCommit)
  - deleteuser/deleteuser.go:229 (startMetaLoad)
  - deleteuser/deleteuser.go:464 (startSubmit)
  - applysetup/applysetup.go:290 (Init runPreflight)
  - applysetup/applysetup.go:491 (runRePreflight)
  - applysetup/applysetup.go:545 (commit goroutine)
  - pwauthdisable/pwauthdisable.go:290 (Init runPreflight)
  - pwauthdisable/pwauthdisable.go:488 (startSubmit)

Per D-06 / D-07 / D-08: SIGTERM-then-2s-then-SIGKILL; modal stays open with "Cancelling..." indicator; closes only when committedMsg arrives; if SIGKILL still doesn't release within 2s after sending, modal renders the diagnostic "Cancellation failed - subprocess PID=N still alive. Run kill -9 N from another shell." (verbatim copy from D-08 with the live PID injected) and admin can press Esc again to force-close the modal frame.

D-08 PID plumbing (planner's chosen threading): extend `sysops.ExecResult` with a `PID int` field populated by `Exec` after `cmd.Start()` returns. The 4 modal goroutines that wrap a sysops Exec carry the PID forward on their own done-msg type (`committedMsg.pid`, `submitDoneMsg.pid`, `applyDoneMsg.pid`). When the modal's `handleCommitted` (or equivalent) hits the hang branch (cancellation attempted but err is non-context), it formats the D-08 diagnostic with the captured PID. For modals that fan out across multiple sysops calls inside one txn batch (applysetup commit goroutine, deleteuser startSubmit), the goroutine stores the most-recent PID in a model field (`m.lastPID`) so the diagnostic always references the subprocess that was actually mid-flight at Esc time. This avoids extending the SystemOps interface with a callback parameter and keeps the threading inside each modal's existing goroutine body.

Per D-10: cancellation infra is per-site, NOT a shared widget. If drift emerges during execution, planner discretion to extract; default keeps each modal independent.

Per D-21: SAFE-04 revert window contract is preserved unchanged. Any FW mutations in these modals (addkey writes ssh keys; pwauthdisable writes sshd_config drop-in; deleteuser may delete ufw rules) inherit the SAFE-04 wrapping from existing txn.Apply contract. Cancellation is Esc-during-running, NOT Esc-during-validate; SAFE-04's reverse-cmd is what restores state if SIGTERM mid-flight breaks an atomic mutation.

Purpose: closes TUI-11 polish gap; Esc cancels the in-flight subprocess instead of leaking an orphan that keeps mutating; D-07 indicator and D-08 hang diagnostic provide the documented UX contract verbatim.
Output: 1 sysops change (cmd.Cancel + cmd.WaitDelay + ExecResult.PID), 9 site replacements across 4 modal files, 2 sysops timing/PID tests, ~12 modal teatest tests (~3 per modal: cancelling state + modal-stays-open + hang diagnostic), 1 SUMMARY.

Per D-18: this is one of 4 independent plans (one per requirement); plans are parallelizable in any order.
Per D-22: every task uses atomic commit-per-step (RED test commit -> GREEN implementation commit -> SUMMARY commit at plan close).
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-CONTEXT.md
@.planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-RESEARCH.md
@CLAUDE.md

@internal/sysops/real.go
@internal/sysops/sysops.go
@internal/tui/screens/observerun/observerun.go
@internal/tui/screens/addkey/addkey.go
@internal/tui/screens/deleteuser/deleteuser.go
@internal/tui/screens/applysetup/applysetup.go
@internal/tui/screens/pwauthdisable/pwauthdisable.go

<interfaces>
From internal/sysops/sysops.go (existing ExecResult struct ~line 44):

  type ExecResult struct {
      Stdout   []byte
      Stderr   []byte
      ExitCode int
      PID      int  // NEW (TUI-11 D-08): live subprocess PID captured after cmd.Start, used for hang-escalation diagnostic.
  }

From internal/sysops/real.go (existing Exec at line 151; full body 151-215). Two coupled changes:

  (A) The KEY change is between lines 191 (exec.CommandContext) and 196 (cmd.CombinedOutput):

  cmd := exec.CommandContext(ctx, bin, args...)
  cmd.Stdin = nil

  // NEW (TUI-11) - SIGTERM-first cancellation policy per D-06, Cmd.Cancel
  // semantics stable since Go 1.20. Pitfall 1: Signal returning
  // os.ErrProcessDone is the "process already self-exited" race - return
  // nil so Wait does not surface a spurious cancellation error.
  cmd.Cancel = func() error {
      if cmd.Process == nil { return nil }
      err := cmd.Process.Signal(syscall.SIGTERM)
      if errors.Is(err, os.ErrProcessDone) { return nil }
      return err
  }
  // 2s grace then automatic SIGKILL via stdlib (D-06).
  cmd.WaitDelay = 2 * time.Second

  start := time.Now()
  // ... existing post-processing unchanged ...

  (B) PID capture for D-08. Replace `out, err := cmd.CombinedOutput()` with manual Start + Wait so PID lands in ExecResult before Wait returns:

  var buf bytes.Buffer
  cmd.Stdout = &buf
  cmd.Stderr = &buf
  if err := cmd.Start(); err != nil {
      // Start failed: no PID, return error wrapping unchanged.
      return ExecResult{}, fmt.Errorf("sysops.Exec: cmd.Start: %w", err)
  }
  pid := cmd.Process.Pid  // captured BEFORE Wait (load-bearing for D-08)
  err := cmd.Wait()
  out := buf.Bytes()
  // ... existing post-processing populates res.Stdout / res.ExitCode / res.Stderr ...
  res.PID = pid

This single edit propagates cancellation semantics + PID capture to ALL sysops Exec call sites.

Required imports in internal/sysops/real.go: syscall (for SIGTERM), os (for ErrProcessDone), errors (for Is), bytes (for buf if not already imported). Verify imports list at top of file before editing; addkey-style modal sites do NOT import os/exec or syscall (sysops invariant via scripts/check-no-exec-outside-sysops.sh).

From internal/tui/screens/observerun/observerun.go (production reference at line 198-200):

  func (m *Model) startSubprocess() tea.Cmd {
      return func() tea.Msg {
          ctx, cancel := context.WithCancel(context.Background())
          m.cancelFn = cancel
          // ... goroutine using ctx ...
      }
  }

The 9 sites currently look like (verified via grep):

  // addkey.go:380 (resolveAsync)
  ctx, cancel := context.WithTimeout(context.Background(), fetchTimeout)

  // addkey.go:532 (attemptCommit)
  ctx, cancel := context.WithTimeout(context.Background(), commitTimeout)

  // deleteuser.go:229 (startMetaLoad)
  ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)

  // deleteuser.go:464 (startSubmit)
  ctx, cancel := context.WithTimeout(context.Background(), 60*time.Second)

  // applysetup.go:290 (Init runPreflight)
  ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)

  // applysetup.go:491 (runRePreflight)
  ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)

  // applysetup.go:545 (commit goroutine)
  ctx, cancel := context.WithTimeout(context.Background(), 60*time.Second)

  // pwauthdisable.go:290 (Init runPreflight)
  ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)

  // pwauthdisable.go:488 (startSubmit)
  ctx, cancel := context.WithTimeout(context.Background(), 60*time.Second)

REPLACEMENT pattern (per RESEARCH "TUI-11 per-site"):

  // (a) base cancellable context
  ctx, cancel := context.WithCancel(context.Background())
  m.cancelFn = cancel
  // (b) preserve the existing deadline by chaining WithTimeout (the
  //     timeout adds an extra cancel func that we don't need to store -
  //     the WithCancel cancel is what Esc invokes)
  ctx, _ = context.WithTimeout(ctx, <existing_timeout>)

  // tea.Batch(m.spinner.Tick, func() tea.Msg {
  //     defer cancel()  // safety net for happy-path cleanup
  //     ... existing body; capture result.PID into a local pid var; surface on done-msg ...
  // })

The Esc-during-async handler (per modal):

  func (m *Model) handleEsc() (nav.Screen, tea.Cmd) {
      if m.phase == phaseCommitting || m.phase == phaseFetching || m.phase == phasePreflighting {
          if m.cancelFn != nil { m.cancelFn() }
          m.cancelling = true
          // Modal STAYS OPEN per D-07; closes when committedMsg arrives.
          return m, nil
      }
      // existing pre-async Esc behaviour: pop modal frame
      return m, nav.PopCmd()
  }

The done-msg type per modal (each modal already has its own; extend with pid):

  // addkey: existing committedMsg{ err error }
  type committedMsg struct {
      err error
      pid int  // NEW (TUI-11 D-08): live PID captured by goroutine before Wait returns; surfaced on hang.
  }

  // deleteuser: existing submitDoneMsg{ err error } -> add pid int
  // applysetup: existing applyDoneMsg{ err error }  -> add pid int
  // pwauthdisable: existing submitDoneMsg{ err error } -> add pid int

The handleCommitted / done-handler must distinguish cancelled from success/fatal AND render the D-08 verbatim diagnostic on hang:

  func (m *Model) handleCommitted(msg committedMsg) (nav.Screen, tea.Cmd) {
      if m.cancelling {
          if msg.err == nil || errors.Is(msg.err, context.Canceled) || errors.Is(msg.err, context.DeadlineExceeded) {
              m.phase = phaseError  // OR phaseCancelled if a new state is added
              m.errInline = "cancelled by Esc"
              m.errFatal = false
              m.cancelling = false
              return m, nil  // optional autoPopDelay
          }
          // D-08 hang escalation: cancellation attempted but subprocess
          // returned a non-context error - SIGKILL did not release within 2s
          // after sending. Render the D-08 verbatim copy with the live PID.
          // Substrings 'Cancellation failed', 'Run kill -9', 'from another
          // shell.' MUST land verbatim per CONTEXT.md D-08.
          m.phase = phaseError
          m.errInline = fmt.Sprintf("Cancellation failed - subprocess PID=%d still alive. Run kill -9 %d from another shell.", msg.pid, msg.pid)
          m.errFatal = true
          m.cancelling = false
          return m, nil
      }
      // existing success / error branches unchanged
  }

The View() during cancellation must render the 'Cancelling...' indicator (D-07):

  func (m *Model) View() string {
      // ... existing render ...
      if m.cancelling {
          // D-07: 'Cancelling...' indicator. Spinner-prefixed if widgets.Spinner
          // is already in scope; otherwise static text. Substring 'Cancelling'
          // MUST land in View() output verbatim per CONTEXT.md D-07.
          body = lipgloss.JoinVertical(lipgloss.Left, body, m.spinner.View()+" Cancelling...")
      }
      // ... existing modal frame ...
  }

The struct field additions per modal:

  type Model struct {
      // ... existing fields ...
      cancelFn   context.CancelFunc  // NEW (TUI-11)
      cancelling bool                // NEW (TUI-11) - true after Esc dispatched cancel
      lastPID    int                 // NEW (TUI-11 D-08) - most-recent subprocess PID; refreshed on each sysops Exec inside this modal's goroutine; passed to done-msg.
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED + GREEN - sysops.Exec gains cmd.Cancel = SIGTERM, cmd.WaitDelay = 2s, ExecResult.PID</name>
  <read_first>
    - internal/sysops/real.go (full file - Exec at line 151-215; allowlist at lines 100-180; existing imports list at top of file)
    - internal/sysops/sysops.go (full file - ExecResult struct definition at line 44)
    - internal/sysops/real_test.go (full file - existing Exec test patterns; identify how /bin/sleep or similar is used in tests; if no such test exists, mirror the closest stdlib test idiom)
    - https://pkg.go.dev/os/exec (Cmd.Cancel and Cmd.WaitDelay reference - already cited in RESEARCH)
    - .planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-RESEARCH.md "Common Pitfalls" Pitfall 1 + 2 (cmd.Cancel error semantics)
  </read_first>
  <files>internal/sysops/sysops.go, internal/sysops/real.go, internal/sysops/real_test.go</files>
  <behavior>
    Test 1: TestExec_cancel_sends_SIGTERM_then_SIGKILL_after_2s
    - Use a long-running subprocess (e.g. /bin/sleep 30 if /bin/sleep is allowlisted; otherwise pre-add it to the test allowlist or use a test-specific path).
    - Spawn via Real.Exec(ctx, "/bin/sleep", "30") in a goroutine.
    - After 100ms, cancel the parent ctx.
    - Assert: Exec returns within ~2.5s (well before sleep's 30s).
    - Assert: returned error is errors.Is(err, context.Canceled) OR errors.Is(err, context.DeadlineExceeded).
    - This pins both the SIGTERM delivery (sleep 30 doesn't naturally exit) and the WaitDelay 2s upper bound.

    Test 2: TestExec_clean_exit_after_cancel_returns_canceled_not_failed
    - Spawn /bin/true (which exits immediately with 0).
    - Cancel after 5ms.
    - Assert: NO panic; the returned error is either nil OR errors.Is(err, context.Canceled). Pitfall 1 - Signal returning os.ErrProcessDone is NOT surfaced as a spurious error.

    Test 3: TestExec_returns_pid_after_start (D-08 PID plumbing)
    - Spawn /bin/sleep 1 (short enough to not slow tests).
    - Assert: result.PID > 0 (the field is populated, regardless of subsequent Wait outcome).
    - Assert: result.PID matches a process that existed (not strictly testable without race; the > 0 check is the load-bearing assertion).

    Test 4 (regression guard): TestExec_normal_exec_still_works
    - Existing test - re-run; spawn /bin/echo or whatever is allowlisted; assert ExitCode==0 and Stdout matches expectation; also assert result.PID > 0 to lock the new field on every Exec call.
  </behavior>
  <action>
    Step 1 (RED) - In internal/sysops/real_test.go, add Test 1, Test 2, and Test 3 above. Update Test 4 (existing) to also assert PID > 0. If /bin/sleep / /bin/true is not in the existing allowlist for sysops, you may need to:
    - (a) Use the existing test-mode allowlist seam if one exists; OR
    - (b) Add a test-only helper that exposes a non-allowlist Exec path (e.g. testExec(t, *Real, ...) that bypasses the allowlist for whitelisted test-only paths "/bin/sleep" and "/bin/true").

    Existing real_test.go MUST already have a way to test /bin/<thing> calls; mirror that pattern. Do NOT introduce a new test seam without first verifying nothing already exists.

    Run go test ./internal/sysops/ -run "TestExec_cancel|TestExec_clean_exit|TestExec_returns_pid" -count=1 - confirm RED (Test 1 will fail because default Cancel is SIGKILL with no grace; Test 3 will fail because ExecResult.PID does not yet exist; Test 2 may pass-by-luck depending on timing).

    Commit: test(06-04): RED - Exec cancel SIGTERM + WaitDelay 2s + ExecResult.PID

    Step 2 (GREEN) - Two coupled edits in sysops:

    (A) In internal/sysops/sysops.go, extend ExecResult:

       type ExecResult struct {
           Stdout   []byte
           Stderr   []byte
           ExitCode int
           PID      int  // TUI-11 D-08: live subprocess PID captured after cmd.Start; used for hang-escalation diagnostic.
       }

    (B) In internal/sysops/real.go, modify Exec between line 191 and line 196:

    a) Ensure imports include syscall, os, errors, bytes. Existing file should already have os, errors; verify syscall and bytes; add as needed.

    b) After cmd := exec.CommandContext(ctx, bin, args...) and cmd.Stdin = nil, before start := time.Now(), insert the Cancel + WaitDelay block:

       // TUI-11 D-06: SIGTERM-first cancellation policy. Default Cmd.Cancel
       // is SIGKILL on ctx cancel; we override to SIGTERM and let
       // WaitDelay handle the 2s SIGKILL fallback per Go 1.20+ semantics.
       // Pitfall 1: os.ErrProcessDone from Signal means the process
       // already self-exited (Cancel-vs-exit race) - returning nil
       // prevents Wait from surfacing a spurious cancellation error.
       cmd.Cancel = func() error {
           if cmd.Process == nil {
               return nil
           }
           err := cmd.Process.Signal(syscall.SIGTERM)
           if errors.Is(err, os.ErrProcessDone) {
               return nil
           }
           return err
       }
       cmd.WaitDelay = 2 * time.Second

    c) Replace `out, err := cmd.CombinedOutput()` (line ~196) with manual Start + Wait so PID is captured before Wait returns:

       // TUI-11 D-08: switch from CombinedOutput to Start + Wait so that
       // cmd.Process.Pid is captured BEFORE Wait returns. This makes the
       // PID available on hang (where Wait may return after WaitDelay
       // SIGKILL fallback fires).
       var buf bytes.Buffer
       cmd.Stdout = &buf
       cmd.Stderr = &buf
       if startErr := cmd.Start(); startErr != nil {
           return ExecResult{}, fmt.Errorf("sysops.Exec: cmd.Start: %w", startErr)
       }
       pid := cmd.Process.Pid  // captured BEFORE Wait (load-bearing for D-08)
       err := cmd.Wait()
       out := buf.Bytes()

    d) In the existing post-processing, populate `res.PID = pid` alongside Stdout / ExitCode / Stderr.

    Run go test ./internal/sysops/ -count=1 -v - all tests including new ones must pass.

    Commit: feat(06-04): sysops.Exec sends SIGTERM with 2s SIGKILL grace; ExecResult.PID populated
  </action>
  <verify>
    <automated>cd /Users/jnuyens/sftp-jailer &amp;&amp; go test ./internal/sysops/ -count=1 -timeout 60s</automated>
  </verify>
  <acceptance_criteria>
    - sysops.go ExecResult has PID field: grep -q 'PID *int' /Users/jnuyens/sftp-jailer/internal/sysops/sysops.go
    - real.go contains the Cancel closure: grep -q "cmd.Cancel = func()" /Users/jnuyens/sftp-jailer/internal/sysops/real.go && grep -q "syscall.SIGTERM" /Users/jnuyens/sftp-jailer/internal/sysops/real.go && grep -q "os.ErrProcessDone" /Users/jnuyens/sftp-jailer/internal/sysops/real.go
    - real.go sets WaitDelay = 2s: grep -q "cmd.WaitDelay = 2 \* time.Second" /Users/jnuyens/sftp-jailer/internal/sysops/real.go
    - real.go captures PID before Wait: grep -q "cmd.Process.Pid" /Users/jnuyens/sftp-jailer/internal/sysops/real.go
    - real.go imports syscall: grep -E '"syscall"' /Users/jnuyens/sftp-jailer/internal/sysops/real.go
    - All three new tests defined: grep -q "TestExec_cancel_sends_SIGTERM_then_SIGKILL_after_2s" /Users/jnuyens/sftp-jailer/internal/sysops/real_test.go && grep -q "TestExec_clean_exit_after_cancel_returns_canceled_not_failed" /Users/jnuyens/sftp-jailer/internal/sysops/real_test.go && grep -q "TestExec_returns_pid_after_start" /Users/jnuyens/sftp-jailer/internal/sysops/real_test.go
    - All sysops tests green within 60s timeout: go test ./internal/sysops/ -count=1 -timeout 60s exits 0
    - sysops invariant intact: bash /Users/jnuyens/sftp-jailer/scripts/check-no-exec-outside-sysops.sh
    - Em-dash absent: ! grep -P '[\x{2014}\x{2013}]' /Users/jnuyens/sftp-jailer/internal/sysops/real.go /Users/jnuyens/sftp-jailer/internal/sysops/real_test.go /Users/jnuyens/sftp-jailer/internal/sysops/sysops.go
    - Two atomic commits: test(06-04): and feat(06-04):
  </acceptance_criteria>
  <done>sysops.Exec adopts cmd.Cancel SIGTERM + cmd.WaitDelay 2s policy and populates ExecResult.PID; tests prove SIGTERM+SIGKILL timing under cancellation, PID surfacing on every Exec, and clean-exit-after-cancel race handled per Pitfall 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED + GREEN - replace 9 detached context.Background() sites with stored cancellable contexts; add Esc-during-async handlers, Cancelling indicator (D-07), and D-08 hang diagnostic</name>
  <read_first>
    - All 4 modal files in full: internal/tui/screens/{addkey,deleteuser,applysetup,pwauthdisable}/*.go (so the executor sees the surrounding phase machine, struct fields, handleKey switch, attemptCommit/resolveAsync/runPreflight bodies, View() bodies for indicator placement)
    - internal/tui/screens/observerun/observerun.go lines 70-220 (production reference for stored cancelFn + Esc handler + handleSubprocessDone with cancelling-state branch + 'Cancelling...' indicator render)
    - .planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-RESEARCH.md Patterns 1+2 and Pitfall 1+2 (cancellation error classification)
    - .planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-CONTEXT.md D-07 (Cancelling indicator) and D-08 (verbatim hang diagnostic copy)
    - All 4 modal _test.go files (existing teatest patterns + phase-machine assertions)
    - Re-run grep -n "context.Background()" /Users/jnuyens/sftp-jailer/internal/tui/screens/{addkey,deleteuser,applysetup,pwauthdisable}/*.go right before editing to confirm the 9 sites have not shifted line numbers. Update the in-action plan if line numbers drifted.
  </read_first>
  <files>internal/tui/screens/addkey/addkey.go, internal/tui/screens/addkey/addkey_test.go, internal/tui/screens/deleteuser/deleteuser.go, internal/tui/screens/deleteuser/deleteuser_test.go, internal/tui/screens/applysetup/applysetup.go, internal/tui/screens/applysetup/applysetup_test.go, internal/tui/screens/pwauthdisable/pwauthdisable.go, internal/tui/screens/pwauthdisable/pwauthdisable_test.go</files>
  <behavior>
    Per modal, add at least 3 teatest tests:

    Test A: TestEsc_during_async_invokes_cancel_and_renders_cancelling_indicator (D-07)
    - Drive the modal into its phaseCommitting or phaseFetching state via the modal's normal entry flow.
    - Inject a stub sysops.SystemOps that blocks on a long-running call (e.g. ExecResponseQueue with a delay or a channel-backed Exec stub).
    - Send tea.KeyPressMsg{ Code: 'esc' }.
    - Assert: m.cancelFn was invoked (verify via a recorded counter on the stub or by checking m.cancelling==true via a test-export).
    - Assert: View() (or the modal's render output) contains substring 'Cancelling' (D-07 indicator render, load-bearing for the warning closure).
    - Assert: modal STAYS OPEN (View() still renders the modal frame, not a popped state).
    - Assert subsequent committedMsg arrival (success or error) closes the modal.

    Test B: TestEsc_modal_stays_open_until_subprocess_exits_with_cancelled_classification (Pitfall 2)
    - Same setup; after Esc -> cancelFn fires -> stub Exec finally returns context.Canceled.
    - Assert: handleCommitted observes the err and renders neutral cancelled state ("cancelled by Esc"); modal then auto-pops or stays in cancelled-state per existing pattern.
    - Pin Pitfall 2: cancellation MUST NOT trigger the success-toast path.

    Test C: TestEsc_hang_diagnostic_renders_verbatim_d08_copy_with_pid (D-08)
    - Setup: scripted Fake/Stub Exec that returns a non-context error AFTER cancellation (simulating SIGKILL fallback failing to clear; the error is e.g. errors.New("ETIMEDOUT") or similar, NOT context.Canceled / context.DeadlineExceeded).
    - The stub also returns ExecResult{PID: 12345} so the modal goroutine has a PID to thread onto its done-msg.
    - Drive the modal into phaseCommitting; send Esc; await committedMsg (or equivalent done-msg).
    - Assert View() (or m.errInline directly via test-export) contains substring 'Cancellation failed - subprocess PID='
    - Assert View() contains substring 'Run kill -9 12345' (PID-formatted; the exact PID makes the assertion specific)
    - Assert View() contains substring 'from another shell.'
    - This may be factored as a single table-driven test across all 4 modals if the test harness supports it; otherwise duplicate per modal.

    Total: 4 modals * 3 tests = 12 new tests. For applysetup which has 3 sites (290, 491, 545), Test A may exercise either preflight or commit; the runRePreflight site can be covered implicitly via the entry flow.
  </behavior>
  <action>
    Step 1 (RED, modal-by-modal) - For each of the 4 modals, add Test A + Test B + Test C in the corresponding _test.go. Mirror the observerun_test.go cancellation-test idiom verbatim (it's the production reference). Use teatest's Send + Wait + ExpectFinalView patterns.

    Run go test ./internal/tui/screens/addkey/... ./internal/tui/screens/deleteuser/... ./internal/tui/screens/applysetup/... ./internal/tui/screens/pwauthdisable/... -count=1 -v - confirm RED (modals don't yet have m.cancelFn / m.cancelling / lastPID / Cancelling indicator / D-08 diagnostic).

    Commit: test(06-04): RED - Esc-during-async cancellation + D-07 indicator + D-08 hang tests for 4 modals

    Step 2 (GREEN, modal-by-modal) - For EACH of the 4 modal files:

    a) Add struct fields:
         cancelFn   context.CancelFunc
         cancelling bool
         lastPID    int  // D-08: most-recent sysops Exec PID for hang diagnostic.

    b) Extend the modal's done-msg type with a pid field:
         // addkey:
         type committedMsg struct{ err error; pid int }
         // deleteuser:
         type submitDoneMsg struct{ err error; pid int }
         // applysetup:
         type applyDoneMsg struct{ err error; pid int }
         // pwauthdisable:
         type submitDoneMsg struct{ err error; pid int }

    c) For EACH context.Background() site (9 total), apply the replacement pattern:

       BEFORE:
         ctx, cancel := context.WithTimeout(context.Background(), <existing_timeout>)
         defer cancel()  // happy-path cleanup
         // ... body using ops.Exec(ctx, ...) ...
         return committedMsg{err: err}

       AFTER:
         baseCtx, cancel := context.WithCancel(context.Background())
         m.cancelFn = cancel
         ctx, _ := context.WithTimeout(baseCtx, <existing_timeout>)
         defer cancel()  // safety net; idempotent
         // ... body unchanged ... but capture pid from each sysops Exec result:
         //     res, err := ops.Exec(ctx, ...)
         //     if res.PID != 0 { m.lastPID = res.PID; lastPID = res.PID }
         // (m.lastPID write is racy across goroutines; the local lastPID
         //  variable threaded onto the done-msg is the load-bearing PID
         //  source. m.lastPID is best-effort breadcrumb only.)
         return committedMsg{err: err, pid: lastPID}

       For paths that wrap a txn batch (applysetup commit, deleteuser submit) where multiple sysops Exec calls fire sequentially: the goroutine captures the most-recent ExecResult.PID into the local lastPID variable on each Exec call; whichever Exec was mid-flight at Esc time is the one whose PID surfaces on the done-msg.

       Notes:
       - The existing timeout is preserved exactly (fetchTimeout / commitTimeout / 30s / 60s / 10s as listed in interfaces).
       - Storing m.cancelFn is the load-bearing change for cancellation - it gives Esc a handle to the same cancel as defer cancel().
       - Calling cancel() twice is safe (CancelFunc is idempotent).
       - Storing lastPID local variable + threading onto done-msg is the load-bearing change for D-08 diagnostic.

    d) In handleKey (or the equivalent key dispatcher), add an Esc-during-async branch BEFORE any pre-existing Esc-pops-modal branch:

         case "esc":
             // TUI-11: Esc during async phase cancels the in-flight subprocess.
             if m.phase == phaseCommitting || m.phase == phaseFetching || m.phase == phasePreflighting {
                 if m.cancelFn != nil {
                     m.cancelFn()
                 }
                 m.cancelling = true
                 // Modal stays open per D-07; closes when committedMsg arrives.
                 return m, nil
             }
             // existing pre-async Esc behaviour (pop modal frame) unchanged
             return m, nav.PopCmd()

       Use the modal's actual phase enum constants (phaseCommitting / phaseFetching / phasePreflighting are placeholders; resolve to the modal's real phase names).

    e) In handleCommitted (or the equivalent done-handler) add the cancellation classification branch FIRST, BEFORE the success/fatal branches (Pitfall 2). The hang branch MUST format the D-08 verbatim copy with the live PID injected:

         if m.cancelling {
             if msg.err == nil || errors.Is(msg.err, context.Canceled) || errors.Is(msg.err, context.DeadlineExceeded) {
                 m.phase = phaseError  // OR a new phaseCancelled if the modal benefits
                 m.errInline = "cancelled by Esc"
                 m.errFatal = false
                 m.cancelling = false
                 return m, autoPopAfterDelay()
             }
             // D-08 (CONTEXT.md verbatim): cancellation attempted but
             // subprocess returned a non-context error - SIGKILL did not
             // release within 2s after sending. The substrings
             // 'Cancellation failed', 'Run kill -9', and 'from another
             // shell.' MUST land verbatim per D-08 lock.
             m.phase = phaseError
             m.errInline = fmt.Sprintf("Cancellation failed - subprocess PID=%d still alive. Run kill -9 %d from another shell.", msg.pid, msg.pid)
             m.errFatal = true
             m.cancelling = false
             return m, nil
         }
         // existing success / fatal branches unchanged

       Note the EXACT string format - this is checker-asserted via grep on the source file. The substrings 'Cancellation failed', 'Run kill -9', and 'from another shell' must appear verbatim. Do NOT reword.

    f) In View() (or the modal's render helper), if m.cancelling==true and the modal is mid-async, render a "Cancelling..." indicator. Mirror observerun.go's existing cancellation indicator if present. Minimum acceptable: append the static text "Cancelling..." (with the leading capital C, ellipsis as three ASCII dots, NOT U+2026) onto the existing spinner / status line. The substring 'Cancelling' MUST appear in View() output - this is checker-asserted via grep on the source file.

       Suggested render (each modal adapts to its own View() shape):

         if m.cancelling {
             // D-07: 'Cancelling...' indicator. Static text with spinner prefix.
             body = lipgloss.JoinVertical(lipgloss.Left, body, m.spinner.View()+" Cancelling...")
         }

    g) Per the Architectural Responsibility Map: the modal does NOT import os/exec or syscall. The cancellation surface stays in sysops. Run bash /Users/jnuyens/sftp-jailer/scripts/check-no-exec-outside-sysops.sh after edits.

    Step 3 - Apply per-modal in this commit order to keep diffs reviewable:
       (i)   addkey  -> 2 sites, 3 tests; commit feat(06-04): TUI-11 cancellable addkey resolveAsync + commit (D-07 indicator + D-08 diagnostic)
       (ii)  deleteuser -> 2 sites, 3 tests; commit feat(06-04): TUI-11 cancellable deleteuser metaLoad + submit (D-07 + D-08)
       (iii) applysetup -> 3 sites, 3 tests; commit feat(06-04): TUI-11 cancellable applysetup preflight + commit (D-07 + D-08)
       (iv)  pwauthdisable -> 2 sites, 3 tests; commit feat(06-04): TUI-11 cancellable pwauthdisable preflight + submit (D-07 + D-08)

    After each modal commit, run: go test ./internal/tui/screens/<modal>/... -count=1 - must be green before moving to the next modal.

    Step 4 (sweep) - Run grep -n "context.Background()" /Users/jnuyens/sftp-jailer/internal/tui/screens/{addkey,deleteuser,applysetup,pwauthdisable}/*.go - expect ZERO results in non-test files. Test files MAY still use context.Background() for fixture setup; that's fine.
  </action>
  <verify>
    <automated>cd /Users/jnuyens/sftp-jailer &amp;&amp; go test ./internal/tui/screens/addkey/... ./internal/tui/screens/deleteuser/... ./internal/tui/screens/applysetup/... ./internal/tui/screens/pwauthdisable/... ./internal/sysops/... -count=1 -timeout 120s &amp;&amp; ! grep -n "context.Background()" /Users/jnuyens/sftp-jailer/internal/tui/screens/addkey/addkey.go /Users/jnuyens/sftp-jailer/internal/tui/screens/deleteuser/deleteuser.go /Users/jnuyens/sftp-jailer/internal/tui/screens/applysetup/applysetup.go /Users/jnuyens/sftp-jailer/internal/tui/screens/pwauthdisable/pwauthdisable.go</automated>
  </verify>
  <acceptance_criteria>
    - Zero context.Background() in the 4 production modal files: ! grep -nH "context.Background()" /Users/jnuyens/sftp-jailer/internal/tui/screens/addkey/addkey.go /Users/jnuyens/sftp-jailer/internal/tui/screens/deleteuser/deleteuser.go /Users/jnuyens/sftp-jailer/internal/tui/screens/applysetup/applysetup.go /Users/jnuyens/sftp-jailer/internal/tui/screens/pwauthdisable/pwauthdisable.go
    - All 4 modals declare cancelFn field: for f in addkey/addkey.go deleteuser/deleteuser.go applysetup/applysetup.go pwauthdisable/pwauthdisable.go; do grep -q "cancelFn *context.CancelFunc" /Users/jnuyens/sftp-jailer/internal/tui/screens/$f || exit 1; done
    - All 4 modals declare cancelling bool: for f in addkey/addkey.go deleteuser/deleteuser.go applysetup/applysetup.go pwauthdisable/pwauthdisable.go; do grep -q "cancelling *bool" /Users/jnuyens/sftp-jailer/internal/tui/screens/$f || exit 1; done
    - All 4 modals declare lastPID int: for f in addkey/addkey.go deleteuser/deleteuser.go applysetup/applysetup.go pwauthdisable/pwauthdisable.go; do grep -q "lastPID *int" /Users/jnuyens/sftp-jailer/internal/tui/screens/$f || exit 1; done
    - All 4 modals have an Esc-during-phase handler that calls m.cancelFn(): for f in addkey/addkey.go deleteuser/deleteuser.go applysetup/applysetup.go pwauthdisable/pwauthdisable.go; do grep -q "m.cancelFn()" /Users/jnuyens/sftp-jailer/internal/tui/screens/$f || exit 1; done
    - All 4 modals classify cancellation via errors.Is(.., context.Canceled): for f in addkey/addkey.go deleteuser/deleteuser.go applysetup/applysetup.go pwauthdisable/pwauthdisable.go; do grep -q "errors.Is" /Users/jnuyens/sftp-jailer/internal/tui/screens/$f || exit 1; done
    - D-07: All 4 modals render the 'Cancelling' indicator string in source: grep -q 'Cancelling' /Users/jnuyens/sftp-jailer/internal/tui/screens/addkey/addkey.go && grep -q 'Cancelling' /Users/jnuyens/sftp-jailer/internal/tui/screens/deleteuser/deleteuser.go && grep -q 'Cancelling' /Users/jnuyens/sftp-jailer/internal/tui/screens/applysetup/applysetup.go && grep -q 'Cancelling' /Users/jnuyens/sftp-jailer/internal/tui/screens/pwauthdisable/pwauthdisable.go
    - D-08: All 4 modals contain the verbatim 'Run kill -9' substring of the hang diagnostic: grep -q 'Run kill -9' /Users/jnuyens/sftp-jailer/internal/tui/screens/addkey/addkey.go && grep -q 'Run kill -9' /Users/jnuyens/sftp-jailer/internal/tui/screens/deleteuser/deleteuser.go && grep -q 'Run kill -9' /Users/jnuyens/sftp-jailer/internal/tui/screens/applysetup/applysetup.go && grep -q 'Run kill -9' /Users/jnuyens/sftp-jailer/internal/tui/screens/pwauthdisable/pwauthdisable.go
    - D-08: All 4 modals contain the verbatim 'Cancellation failed' substring: grep -q 'Cancellation failed' /Users/jnuyens/sftp-jailer/internal/tui/screens/addkey/addkey.go && grep -q 'Cancellation failed' /Users/jnuyens/sftp-jailer/internal/tui/screens/deleteuser/deleteuser.go && grep -q 'Cancellation failed' /Users/jnuyens/sftp-jailer/internal/tui/screens/applysetup/applysetup.go && grep -q 'Cancellation failed' /Users/jnuyens/sftp-jailer/internal/tui/screens/pwauthdisable/pwauthdisable.go
    - D-08: All 4 modals contain the verbatim 'from another shell' substring: grep -q 'from another shell' /Users/jnuyens/sftp-jailer/internal/tui/screens/addkey/addkey.go && grep -q 'from another shell' /Users/jnuyens/sftp-jailer/internal/tui/screens/deleteuser/deleteuser.go && grep -q 'from another shell' /Users/jnuyens/sftp-jailer/internal/tui/screens/applysetup/applysetup.go && grep -q 'from another shell' /Users/jnuyens/sftp-jailer/internal/tui/screens/pwauthdisable/pwauthdisable.go
    - 12+ new tests defined (3 per modal): COUNT=$(grep -c "TestEsc_during_async\|TestEsc_modal_stays_open\|TestEsc_hang_diagnostic" /Users/jnuyens/sftp-jailer/internal/tui/screens/{addkey/addkey_test.go,deleteuser/deleteuser_test.go,applysetup/applysetup_test.go,pwauthdisable/pwauthdisable_test.go}); test "$COUNT" -ge 12
    - All 4 modal package tests green: go test ./internal/tui/screens/addkey/... ./internal/tui/screens/deleteuser/... ./internal/tui/screens/applysetup/... ./internal/tui/screens/pwauthdisable/... -count=1 exits 0
    - sysops invariant intact: bash /Users/jnuyens/sftp-jailer/scripts/check-no-exec-outside-sysops.sh
    - Em-dash absent: ! grep -P '[\x{2014}\x{2013}]' /Users/jnuyens/sftp-jailer/internal/tui/screens/addkey/addkey.go /Users/jnuyens/sftp-jailer/internal/tui/screens/deleteuser/deleteuser.go /Users/jnuyens/sftp-jailer/internal/tui/screens/applysetup/applysetup.go /Users/jnuyens/sftp-jailer/internal/tui/screens/pwauthdisable/pwauthdisable.go
    - 5 atomic commits: test(06-04): + 4x feat(06-04): one per modal
  </acceptance_criteria>
  <done>9 detached contexts replaced; all 4 modals route Esc to cancelFn; D-07 'Cancelling...' indicator renders during cancellation phase; D-08 verbatim hang diagnostic ('Cancellation failed - subprocess PID=N still alive. Run kill -9 N from another shell.') with live PID injected lands when SIGKILL fails to clear; cancellation classified via errors.Is(context.Canceled) BEFORE success/fatal branches; 12+ teatest tests pass; sysops invariant green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green sweep + SUMMARY for plan 06-04 (TUI-11)</name>
  <read_first>
    - .planning/phases/05-packaging-install-purge-automated-release/05-01-SUMMARY.md (or any 05-*-SUMMARY.md as a structural reference)
    - .planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-CONTEXT.md (D-06..D-10, D-19, D-21 are TUI-11's locked decisions)
  </read_first>
  <files>.planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-04-SUMMARY.md</files>
  <action>
    Step 1 - Run full sweep:
      go test ./... -race -count=1 -timeout 120s
      bash /Users/jnuyens/sftp-jailer/scripts/check-no-exec-outside-sysops.sh
      bash /Users/jnuyens/sftp-jailer/scripts/check-go-mod-pins.sh
      bash /Users/jnuyens/sftp-jailer/scripts/check-single-tea-program.sh
      go vet ./...

    All must exit 0. If any modal test is flaky under -race, audit and fix before continuing.

    Step 2 - Author 06-04-SUMMARY.md following project SUMMARY conventions. Required sections:
    - Frontmatter: phase, plan, requirements [TUI-11], status (verified), completed_at
    - What shipped: sysops.Exec gains cmd.Cancel = SIGTERM + cmd.WaitDelay = 2s + ExecResult.PID; 9 detached contexts in 4 modals replaced with stored cancellable contexts; Esc-during-async handlers; D-07 'Cancelling...' indicator; D-08 verbatim hang diagnostic with live PID; cancellation-classified done-handlers
    - Decisions honored: D-06 (SIGTERM-then-2s-then-SIGKILL), D-07 (modal stays open with verbatim 'Cancelling...' indicator render), D-08 (verbatim 'Cancellation failed - subprocess PID=N still alive. Run kill -9 N from another shell.' with live PID), D-09 (planner-time enumerated 9 sites, not the 7 in CONTEXT scout), D-10 (per-site infra, no shared widget extracted), D-19 (sysops invariant preserved), D-21 (SAFE-04 contract preserved)
    - Pitfall 1 closed: Cancel returns nil on os.ErrProcessDone (race with self-exit)
    - Pitfall 2 closed: handleCommitted classifies cancellation BEFORE success/fatal so the cancelling flow doesn't accidentally render a success toast
    - Files modified: bullet list of paths actually touched
    - Commits: list of atomic commits in order (~7 expected: test(06-04) + feat(06-04) for sysops + 4 feat(06-04) for the modals + docs(06-04) for SUMMARY)
    - Verification: full -race suite green; sysops invariant green; em-dash absent; zero context.Background() in production modal files; 'Cancelling' substring landed in all 4 modals; 'Run kill -9' / 'Cancellation failed' / 'from another shell' substrings landed in all 4 modals
    - Open items: none - TUI-11 fully closes

    Commit: docs(06-04): plan 06-04 SUMMARY (TUI-11 verified)
  </action>
  <verify>
    <automated>cd /Users/jnuyens/sftp-jailer &amp;&amp; go test ./... -race -count=1 -timeout 120s &amp;&amp; bash scripts/check-no-exec-outside-sysops.sh &amp;&amp; test -f .planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY exists at the listed path: test -f /Users/jnuyens/sftp-jailer/.planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-04-SUMMARY.md
    - SUMMARY references TUI-11: grep -q TUI-11 /Users/jnuyens/sftp-jailer/.planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-04-SUMMARY.md
    - SUMMARY references all relevant decisions: for d in D-06 D-07 D-08 D-09 D-10 D-19 D-21; do grep -q $d /Users/jnuyens/sftp-jailer/.planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-04-SUMMARY.md || exit 1; done
    - SUMMARY documents Pitfall 1 + 2 closure: grep -qE "Pitfall ?1" /Users/jnuyens/sftp-jailer/.planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-04-SUMMARY.md && grep -qE "Pitfall ?2" /Users/jnuyens/sftp-jailer/.planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-04-SUMMARY.md
    - SUMMARY documents D-07 indicator and D-08 diagnostic shipped verbatim: grep -q 'Cancelling' /Users/jnuyens/sftp-jailer/.planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-04-SUMMARY.md && grep -q 'Run kill -9' /Users/jnuyens/sftp-jailer/.planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-04-SUMMARY.md
    - Em-dash absent in SUMMARY: ! grep -P '[\x{2014}\x{2013}]' /Users/jnuyens/sftp-jailer/.planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-04-SUMMARY.md
    - Full -race suite green: go test ./... -race -count=1 -timeout 120s exits 0
    - All 3 architectural CI guards green
    - Commit lands with prefix docs(06-04):
  </acceptance_criteria>
  <done>Full -race suite green across the entire repo; CI guards green; SUMMARY committed honoring D-06..D-10, D-19, D-21 with Pitfalls 1+2 closure documented and D-07/D-08 verbatim copy verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TUI cancelFn -> sysops.Exec subprocess | Esc invokes the modal's stored cancelFn, which cancels the ctx threaded into sysops.Exec. The Cancel closure in Exec then sends SIGTERM; cmd.WaitDelay sends SIGKILL after 2s if the subprocess hasn't exited. The boundary is admin-trusted; the cancel can only kill subprocesses the admin's own TUI session spawned. |
| sysops.Exec -> kernel signal delivery | Standard POSIX signal contract; SIGTERM may be ignored by a misbehaving subprocess (covered by D-08 hang escalation diagnostic). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Denial of Service | hung subprocess after Esc cancel | mitigate | cmd.WaitDelay = 2s -> stdlib SIGKILL is the hard upper bound on post-cancel wall time; D-08 surfaces the verbatim diagnostic with live PID if SIGKILL itself doesn't release within 2s after sending. |
| T-06-04-02 | Tampering | cancellation-vs-self-exit race | mitigate | Cancel closure handles os.ErrProcessDone -> nil per Pitfall 1; clean-exit-after-Cancel returns nil OR context.Canceled (handled by handleCommitted classifier). |
| T-06-04-03 | Information Disclosure | raw subprocess error leaked on cancellation | mitigate | handleCommitted renders neutral "cancelled by Esc" string when err is context.Canceled / DeadlineExceeded; raw subprocess stderr only surfaces if err is unrelated to cancellation, in which case the D-08 verbatim copy substitutes for the raw stderr passthrough. |
| T-06-04-04 | Tampering | SAFE-04 revert window short-circuit | accept | SAFE-04 timer is set in the txn batch BEFORE the cancellable subprocess; if subprocess gets SIGTERM mid-flight, SAFE-04's reverse-cmd is what restores state. Cancellation does NOT run before SAFE-04 schedules. |
| T-06-04-05 | Tampering | Race between SIGTERM-receiver and txn.Compensate | mitigate | ctx.Canceled IS an Apply-error so txn.Compensate hooks fire; FW-09 catch-all delete is intentional-noop Compensate per D-FW-07; cancellation during the delete loop is rolled back via SAFE-04's reverse-cmd. |
| T-06-04-06 | Spoofing | nil-cancelFn dispatch | accept | Pressing Esc with m.cancelFn==nil is a no-op (nil-check guards every site); pre-async Esc branch unchanged. |
| T-06-04-07 | Information Disclosure | stale PID in D-08 diagnostic (PID reuse) | accept | Linux PIDs are reusable but the window between SIGKILL fallback and admin reading the diagnostic is bounded (~seconds in practice). The diagnostic is admin-only (root TUI) and the worst case is a confused 'kill -9' on an unrelated process belonging to root, which the admin already has authority over. Documented honest-failure-mode per D-08 spirit. |
</threat_model>

<verification>
- All sysops + 4-modal package tests green: go test ./internal/sysops/... ./internal/tui/screens/{addkey,deleteuser,applysetup,pwauthdisable}/... -count=1 -timeout 60s
- Full -race suite green: go test ./... -race -count=1 -timeout 120s
- Zero context.Background() in 4 production modal files: grep -n "context.Background()" /Users/jnuyens/sftp-jailer/internal/tui/screens/{addkey,deleteuser,applysetup,pwauthdisable}/*.go | grep -v _test.go returns no output
- D-07 indicator landed: grep -l 'Cancelling' all 4 modal source files
- D-08 verbatim diagnostic landed: grep -l 'Cancellation failed' && grep -l 'Run kill -9' && grep -l 'from another shell' all 4 modal source files
- sysops invariant intact: bash scripts/check-no-exec-outside-sysops.sh
- W-03 keybind regression guards still pass (lowercase d -> S-USER-DETAIL; uppercase D -> M-DELETE-USER-FW-RULES) - guarded by Plan 06-03 tests, must remain green
- Em-dash absent in all touched files
- ~8 atomic commits land with prefixes test(06-04):, feat(06-04):, docs(06-04):
</verification>

<success_criteria>
- internal/sysops/sysops.go ExecResult gains a PID int field; internal/sysops/real.go::Exec sets cmd.Cancel = SIGTERM closure (with os.ErrProcessDone -> nil), cmd.WaitDelay = 2s, and populates ExecResult.PID via cmd.Process.Pid captured after cmd.Start().
- 9 detached context.Background() sites across the 4 modal files replaced with stored cancellable contexts.
- Each of the 4 modals routes Esc-during-async to m.cancelFn() and renders the 'Cancelling...' indicator (D-07); modal stays open per D-07.
- Each of the 4 modals classifies cancellation via errors.Is(err, context.Canceled) BEFORE the success/fatal branches (Pitfall 2).
- Each of the 4 modals renders the D-08 verbatim hang diagnostic ('Cancellation failed - subprocess PID=N still alive. Run kill -9 N from another shell.') with live PID when SIGKILL fails to clear within 2s.
- 3+ teatest tests per modal (12+ total) cover cancellation invocation, modal-stays-open + Cancelling indicator, and D-08 hang diagnostic semantics.
- sysops invariant intact; full -race suite clean.
- 06-04-SUMMARY.md authored documenting D-06..D-10, D-19, D-21 honored and Pitfalls 1+2 closed.
</success_criteria>

<output>
After completion, create .planning/phases/06-v1-1-carry-over-closure-firewall-edge-tui-polish/06-04-SUMMARY.md
</output>
